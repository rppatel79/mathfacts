spring:
  application:
    name: mathfacts
  datasource:
    url: jdbc:postgresql://db:5432/mathdb
    username: mathuser
    password: mathpass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
